<%= form_with(model: @task,local: true) do |form| %>

  <div class="field">
    <%= form.label :タイトル %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
    <%= form.label :内容 %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
  <%= form.submit %>
<% end %>